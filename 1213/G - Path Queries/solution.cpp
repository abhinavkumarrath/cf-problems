// Author: abhinavkumarrath #include <bits/stdc++.h>using namespace std; #pragma GCC optimize("O3,unroll-loops")#pragma GCC target("avx2,bmi,bmi2,lzcnt,popcnt") using ll = long long;using vi = vector<int>;using vll = vector<ll>;using pii = pair<int, int>; #define pb push_back#define all(x) (x).begin(), (x).end()#define sz(x) (int)(x).size()#define fi first#define se second #define rep(i, a, b) for (int i = (a); i < (b); ++i)#define forn(i, n) rep(i, 0, n)#define per(i, a, b) for (int i = (b) - 1; i >= (a); --i)#define rofn(i, n) per(i, 0, n) template<class T> bool chmin(T& a, const T& b) { if (b < a) { a = b; return 1; } return 0; }template<class T> bool chmax(T& a, const T& b) { if (a < b) { a = b; return 1; } return 0; } void yes() { cout << "YES\n"; }void no() { cout << "NO\n"; } const int INF = 1e9;const ll LINF = 1e18; void solve() {    int n, m;    if (!(cin >> n >> m)) return;        vector<array<int, 3>> e(n - 1);    forn(i, n - 1) {        cin >> e[i][1] >> e[i][2] >> e[i][0];    }        vector<pii> q(m);    forn(i, m) {        cin >> q[i].fi;        q[i].se = i;    }        sort(all(e));    sort(all(q));        vi p(n + 1);    vll s(n + 1, 1);    iota(all(p), 0);        ll res = 0;        auto get_p = [&](auto& self, int v) -> int {        return v == p[v] ? v : p[v] = self(self, p[v]);    };        auto get_c = [&](ll cnt) -> ll {        return cnt * (cnt - 1) / 2;    };        auto merge_sets = [&](int u, int v) {        u = get_p(get_p, u);        v = get_p(get_p, v);        if (u != v) {            if (s[u] < s[v]) swap(u, v);            res -= get_c(s[u]);            res -= get_c(s[v]);            s[u] += s[v];            res += get_c(s[u]);            p[v] = u;        }    };        vll ans(m);    int pos = 0;        forn(i, m) {        while (pos < n - 1 && e[pos][0] <= q[i].fi) {            merge_sets(e[pos][1], e[pos][2]);            pos++;        }        ans[q[i].se] = res;    }        forn(i, m) {        cout << ans[i] << (i == m - 1 ? "" : " ");    }    cout << "\n";} int main() {    ios_base::sync_with_stdio(false);    cin.tie(nullptr);        int t = 1;    //cin >> t;    while (t--) {        solve();    }    return 0;}