// Author: abhinavkumarrath #include <bits/stdc++.h>using namespace std; #pragma GCC optimize("O3,unroll-loops")#pragma GCC target("avx2,bmi,bmi2,lzcnt,popcnt") using ll = long long;using vi = vector<int>;using vll = vector<ll>;using pii = pair<int, int>; #define pb push_back#define all(x) (x).begin(), (x).end()#define sz(x) (int)(x).size()#define fi first#define se second#define rep(i, a, b) for (int i = (a); i < (b); ++i)#define forn(i, n) rep(i, 0, n)#define per(i, a, b) for (int i = (b) - 1; i >= (a); --i)#define rofn(i, n) per(i, 0, n) template<class T> bool chmin(T& a, const T& b) { if (b < a) { a = b; return 1; } return 0; }template<class T> bool chmax(T& a, const T& b) { if (a < b) { a = b; return 1; } return 0; } void yes() { cout << "YES\n"; }void no() { cout << "NO\n"; } const int INF = 1e9;const ll LINF = 1e18; const int MAX_NODES = 300000 * 30 + 5;int tr[MAX_NODES][2];int cnt[MAX_NODES];int node_count = 1; void insert(int x) {    int u = 1;    per(i, 0, 30) {        int bit = (x >> i) & 1;        if (!tr[u][bit]) tr[u][bit] = ++node_count;        u = tr[u][bit];        cnt[u]++;    }} int query(int x) {    int u = 1;    int ans = 0;    per(i, 0, 30) {        int bit = (x >> i) & 1;        if (tr[u][bit] && cnt[tr[u][bit]] > 0) {            u = tr[u][bit];            cnt[u]--;        } else {            u = tr[u][1 - bit];            cnt[u]--;            ans |= (1 << i);        }    }    return ans;} void solve() {    int n;    cin >> n;        vi a(n);    forn(i, n) cin >> a[i];        forn(i, n) {        int p;        cin >> p;        insert(p);    }        forn(i, n) {        cout << query(a[i]) << (i == n - 1 ? "" : " ");    }    cout << "\n";} int main() {    ios_base::sync_with_stdio(false);    cin.tie(nullptr);        int t = 1;    while (t--) {        solve();    }    return 0;}